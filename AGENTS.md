# Contributor instructions

Preserve frozen scoring code, configuration, and recorded numerical results. Do not retune a detector on the published evaluation or confirmation clips. All 200 known clips are now inspected data.

Describe v0 as a Farneback looming proxy, not a connectome or biological circuit reproduction. The later local direction-opponent model is separate and must retain its name `proxy_not_zhao_star`. Direction permutations are engineering controls, not connectome ablations.

Keep public README, new result summaries, figures, and social copy in English. Original scoring/reporting implementations are preserved. Do not commit credentials, datasets, raw dashcam pixels, environment folders, or video exports. Code is MIT; Nexar data retain their upstream license.

Use saved scores for publication figures. Do not rescore videos merely to redraw a figure. Run relevant tests for code changes and verify numerical claims against the published CSV/JSON artifacts.
